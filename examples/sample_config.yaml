# CRM Medallion Framework Configuration

bronze:
  storage_path: ./data/bronze
  encoding_detection: true

silver:
  output_path: ./data/silver
  batch_size: 1000

gold:
  storage_path: ./data/gold
  enable_rag: false

# Uncomment to enable LLM features
# llm_enabled: true
# llm:
#   model_name: gpt-4
#   temperature: 0.0
#   api_key: ${OPENAI_API_KEY}
#   confidence_threshold: 0.7
#   max_retries: 5

log_level: INFO
max_memory_mb: 1024
chunk_size_mb: 10
